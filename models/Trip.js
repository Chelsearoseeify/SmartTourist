class Trip {
    constructor(id, name, city, startDate, endDate, places) {
        this.id = id
        this.name = name;
        this.city = city;
        this.startDate = startDate;
        this.endDate = endDate;
        this.places = places;
    }
}

export default Trip;